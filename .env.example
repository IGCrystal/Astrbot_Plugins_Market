# 复制此文件为 .env 或 .env.local 后替换为你自己的值

# 运行模式，如需本地 Node 端口运行可保留 node
NITRO_PRESET=node

# GitHub OAuth App 信息
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
GITHUB_CALLBACK_URL=http://localhost:3000/api/auth/callback

# 允许访问的 GitHub 用户名（用逗号分隔，大小写不敏感）
GITHUB_ALLOWED_USERS=your-github-username

# 必须使用随机、32 字符以上的安全字符串
AUTH_COOKIE_SECRET=change_me_to_a_random_long_string

# 会话有效期（秒），默认 7 天，可按需调整
AUTH_SESSION_MAX_AGE=604800

# 对外站点 URL，本地开发可填 http://localhost:3000
NUXT_PUBLIC_SITE_URL=http://localhost:3000

# MongoDB Atlas / 数据分析存储
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=astrbot_plugins
